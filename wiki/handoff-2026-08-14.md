# Handoff — Aug 14, 2026 (Reconstruction)

## What Happened

Full pipeline reconstruction per `wiki/pipeline-spec.md`. All code in Posit.Tools, Posit.Phases, and Posit.Cli was wiped and rebuilt from scratch by 3 subagents working in parallel against the spec. No bandaids — every name, type, and path accounted for in the spec before code was written.

## Build Status

**Full solution builds clean: 0 errors, 0 warnings.**

## Module Inventory (all under 200-line carapace cap except 1)

### Posit.Tools (6 files, 825 lines)
- `Z3Runner.cs` (147) — verify + translate, extracts only module namespace
- `PatternRegistry.cs` (145) — pattern/stub loading, lookup
- `PatternRegistryComposer.cs` (148) — skeleton composition, signatures
- `BotHarness.cs` (155) — clean temp dir, materialize, build, test
- `BotHarnessProjects.cs` (124) — .csproj, .sln, Program.cs generation
- `BotHarnessDocker.cs` (106) — Dockerfile.run, Docker build + run

### Posit.Phases (12 files, 1254 lines)
- `IPhase.cs` (16) — phase interface
- `PhaseController.cs` (21) — dispatch
- `IExternalAbstractions.cs` (33) — IPatternRegistry, IZ3Runner interfaces
- `ArchitecturePhase.cs` (145) — model call, parse contract, ContractScanner
- `ContractScanner.cs` (114) — name validation, correction listing
- `DafnyContractsPhase.cs` (117) — Z3 verify
- `DafnyImplementationPhase.cs` (159) — pre-verified, translate
- `CSharpImplementationPhase.cs` (197) — extern caps, io-shell stubs, wiring, dedup
- `WiringGenerator.cs` (195) — C# type tracking, type conversion, Wire.cs
- `TranslatedCSharpScanner.cs` (154) — read real method signatures
- `QaPhase.cs` (89) — deterministic, no model, metadata only
- `GlobalUsings.cs` (14)

### Posit.Cli (2 files + GlobalUsings, 406 lines)
- `Program.cs` (194) — CLI entry, all 5 phases, harness command
- `PositOrchestrator.cs` (212) — FSM routing, snowball, carapace enforcement
  - NOTE: 12 lines over cap. Needs trimming.

## Wiki (cleaned)
- `pipeline-spec.md` — canonical pipeline spec with naming convention
- `carapace-doctrine.md` — skeleton IS the contract
- `proof-methodology.md` — seed → assemble → test → prove → carve
- `connector-diagnosis.md` — shrunk pipeline, closed loop
- `current-abilities.md` — 17 patterns, 6 stubs, trial scorecard
- `desktop-port-plan.md` — Blazor UI plan
- `trials/trial-specs.md` — T1-T24, Tier 0-3

## What's Kept (untouched)
- Pattern Registry (`patterns/`) — 20 patterns + 6 stubs + 16 C# stub templates
- DafnyRuntime (`src/Posit.DafnyRuntime/`)
- Contracts (`src/Posit.Contracts/`)
- Core (`src/Posit.Core/`) — SessionState, FsmReducer
- Data (`src/Posit.Data/`) — ArtifactRepository, StateStore
- AI (`src/Posit.AI/`) — OllamaModelGateway
- Database — PostgreSQL 18 + pgvector, port 5434

## What's Not Done
- T1 trial has NOT been run against the rebuilt pipeline yet
- PositOrchestrator.cs is 212 lines (12 over cap)
- Posit.Dt (desktop UI) and Posit.Web not rebuilt (deferred)
- Prompts may need updating for the new pipeline

## Next Steps
1. Run T1 trial: `dotnet run --project src/Posit.Cli -- run --spec="..."`
2. Run Docker harness on the session
3. Fix any issues found (should be minimal — spec accounts for all known issues)
4. Run T2-T5 sequentially as each passes
5. Trim PositOrchestrator.cs to under 200 lines

## Git State
- Branch: master
- Latest commit: 26ee9a6 — Rebuild Posit pipeline from spec
- Working tree: clean
- Backup: C:\Users\goldf\backup Posit\ (old code for reference)