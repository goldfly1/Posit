# Handoff — 2026-08-29 — Phase D Rack Complete, Role-Dispatch Next

## Where we are

**Repo:** `C:/Users/goldf/Posit`, master @ `e756faa`, tree clean. Corpus 3/3. Builds green.

**Phase D rack final scorecard:**
- T3 ✅ PASS (a3, 2/2)
- T12 ✅ PASS 3/3 (confirm run; empty-key judge fix `e4c6bab` proven live on tc3)
- T6 ⚠️ 3/4 (tc3 = model arithmetic ceiling, C↔K dual-branch)
- T8 ❌ 0/2 × 3 attempts — different layer each: emitter (a1) → harness (a2) → architect contract (a3)

**Gate verdict:** nominal 3/4 pass. Residual failure class = architect contract quality (Phase E), not plumbing.

## Commits this phase (chronological)

`4a65a5b` single-line interface guard (ContractScanner + prompt) →
`96d2023` stdin token decomposition (`double.Parse(tokens[0])`, tokens[i] routing) →
`f22777e` file-param routing is FILE-ENTRY ONLY (stdin guard) →
`7f898e6` BuildChainedArgs bounds on min(ParamNames, ParamTypes) →
`a529667` file/scalar boundary (first string param = file, subsequent = scalar args[i] passthrough) →
`e756faa` CliArgs on ComponentTestCase (harness passes scalar args after testdata path)

## The honest ledger — processed by the plan but not fixed

Deterministic (stamp in order):
1. **Stub double-read** — file rule reads args[0] as CONTENT then passes into IoShell stub `ReadFile(string path)` which re-reads as PATH (T8 a3 evidence: file-not-found named after log content). Fix: path-param stubs take args[i] raw.
2. **`EscapeShellArg` is a no-op** (BotHarnessDocker.cs:150 `return arg;`) — works only because args are space-free so far. The a3 failure ran through this hole.
3. **Format-bearing expectations have no contract home** — T8 keys demand `ERROR: 2` / `No entries` but emitter prints raw values (`WriteLine(ret1)` on int prints `2`). Schema-level gap: no presentation concept.
4. Single-line interface guard is a 3-line heuristic, not a parser — same-line trailing comments still swallow braces.
5. **QaReport persistence unverified** — no QaReport artifact kind observed in posit_artifacts; judge verdicts live only in harness stdout.

Model-class (phase-gate to Phase E):
6. Architect contract nondeterminism on multi-arg specs (a3 degenerated to 1-component stub, no filter logic).
7. T6 tc3 arithmetic ceiling (model).
8. ImplFixer flip-flop across retries (fixes tc2, breaks tc3).

Unfired paths: regression auto-revert, FedInput-vs-intent divergence note, trials T2–T5/T7/T9–T11 never run.

## The agreed strategy (user-approved)

- **Stamp deterministic bugs in order** — layered failures revealed in sequence; each sealed layer makes diagnosis trustworthy (T8 failure frontier: emitter → harness → contract).
- **One general fix over patch piles**: the role-dispatch refactor — parameter roles (path/content/scalar) and answer-key shape as first-class contract fields, emitter dispatches on role. Covers #1+#2+#3 as one design change, not three speedbumps.
- **Phase E** = deterministic contract-fidelity check (component set covers spec's verbs; degenerate one-component contracts rejected). Kills #6/#7/#8 at the root — they share "nobody verifies the contract against the spec."
- Ride-alongs when convenient: #4/#5 hygiene.

## Next steps (in order)

1. **#12 corpus cases** for `a529667` (file/scalar boundary) and `e756faa` (CliArgs path) — in progress when this handoff was written. Corpus is the regression net; these two fixes currently have NO corpus coverage.
2. **Role-dispatch refactor** (#1+#2+#3), T8 rerun as proof on the rack (per rack protocol: ×3, check in).
3. Phase E design: contract-fidelity gate (wiki/plans/).
4. Widen trials: T2–T5, T7, T9–T11.

## Environment / gotchas

- ds4 (deepseek-v4-flash:cloud) exclusive Posit model. Ollama :11434; OLLAMA_API_KEY can go stale.
- `posit_artifacts.artifacts` column is `payload_json` (not `payload`); `posit_state.sessions` has no `id` column; always narrow artifact queries by session_id.
- Harness generates Dockerfile.run with `--no-cache` and **never prunes** — ~100 posit-run-* images, ~7 GB. Harness session dirs (127 in `%TEMP%/posit-bot-harness/`) also never cleaned. Teardown fix belongs in BotHarness/BotHarnessDocker (companion to Phase E work, user aware).
- Docker probe pattern: cd harness session dir → `docker build --no-cache -f Dockerfile.run -q -t <tag> .` → `docker run --rm <tag> <args>`. ENTRYPOINT takes args directly (no shell wrapping — quotes become part of argv).
- Rack protocol (user, verbatim): "If you think it needs to test out, put it on the rack..... do the same trial 3 times and then check in."
- Probe discipline: never assert `ret0`/`ret1` — generator numbers by counter; use `ret\d+` regex (memory fact 2136).
- Frozen: Bonsai EXTRACT_MODEL untouched; ds4 exclusivity; no TUI/GUI work on unproven pipeline.