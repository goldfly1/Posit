# Handoff — Aug 16, 2026 (Sunday → Monday)

## What Happened This Session

Posit Reboot 3. Picked up from Reboot 2's uncommitted changes. Made all 4 Monday
code fixes, ported the edge case catalog, revamped the trial specs, then ran T1
through the pipeline 3 times, fixing TypeChainChecker and wiring issues each round.

## Build Status

**Full solution builds clean: 0 errors, 0 warnings.**

## Git State

- Branch: master
- Latest commit: 0228f49 — Fix wiring: emit out params for void+out multi-return
- Working tree: clean
- 8 commits this session

## Pipeline Status

**ALL 5 PHASES GREEN** — proven in round 3 (session 1nFs1evEIUKDocg0OYTXyw0000):
- architecture → success (retry 1 — scanner caught 3 dead declarations, model fixed)
- dafny-contracts → success (first try)
- dafny-implementation → success (first try — TypeChainChecker passed!)
- csharp-implementation → success (first try)
- qa → success (first try)

Round 4 (session CWs0LfNfxE-x_if3Y6PYMw0000) hit Ollama 429 — weekly limit reached.

## Docker Harness Status

**NOT YET PASSING.** Last Docker build (session 1nFs1evEIUKDocg0OYTXyw0000) had 1 error:
- `CS7036: ValidateRows` called without `out` params — wiring didn't emit them
- **Fixed in commit 0228f49** — wiring now emits `out Type outVar` for void+out methods
- **Not yet tested** — need fresh pipeline run + Docker harness

## Monday's First Step

1. Run T1 pipeline: `dotnet run --project src/Posit.Cli -- run --spec="A CLI tool that reads a CSV file, parses each line into fields, validates that all rows have the same number of fields, transforms each row into a JSON object with field names from the header row, and prints the JSON array to stdout. The tool takes a file path as input."`
2. Run Docker harness: `dotnet run --project src/Posit.Cli -- harness <new-session-id>`
3. Expect: Docker build succeeds (out-param fix), tests run (may still fail on data flow)

## What Was Fixed This Session

### The 4 Monday Code Fixes (commit d4b0b7c)
1. **Scanner rule** — components with connections don't need patternName (orchestrator)
2. **Cut-out chaining** — row-validator.dfy returns (outRows, isValid) not just verdict
3. **Stubs return right shape** — prompt says use ReadLines, ConvertType throws on scalar wrap
4. **TypeChainChecker** — post-Dafny type chain validation, wired into orchestrator

### TypeChainChecker Iterations (commits 1848221, 82ebf4e)
- Round 1: 3 errors — void+out not handled, string[] not compatible, IndexOf broken
- Round 2: 2 errors — AreCompatible used IndexOf("ISequence",1) which finds position 6, not depth
- Round 3: 0 errors — normalized Dafny. prefix, counted ISequence occurrences for depth

### Wiring Out-Param Fix (commit 0228f49)
- Dafny multi-return → C# `void Method(in, out ret1, out ret2)`
- Wiring was calling `Method(in)` without out params → CS7036
- Now emits `Method(in, out Type outVar0, out Type outVar1)` and chains outVar0 as return

### Edge Case Catalog (commit 9a95044)
- 81 patterns ported from Shepherd: 33 InputValidation + 15 SqlInjection + 12 Concurrency + 21 ApiError
- In `src/Posit.Phases/EdgeCases/` — not wired into QA yet (future work)

### Trial Revamp (commits 11aa5e0, 769c589)
- Old trials: one-sentence descriptions, no test data, no acceptance criteria
- New trials: spike-based with concrete test data, cut-out mapping, edge case hooks
- 12 trials (T1-T12) across 2 tiers + 4 north-star designs
- 3 cut-outs exist, 14 needed for full Tier 0-1 coverage
- Report chart at `wiki/trials/trial-revamp-report.png`

## Compute

Ollama weekly limit reached (429). Resets Monday. ~1% compute used this session.

## Key Files Changed

### Posit.Phases
- `ContractScanner.cs` — skip patternName + declaration/use for connection-bearing components
- `TypeChainChecker.cs` — NEW, post-Dafny type chain validation
- `TranslatedCSharpScanner.cs` — tracks OutParamTypes for Dafny multi-return
- `WiringGenerator.cs` — emits out params for void+out methods, string[]→ISeq conversion, throws on scalar dim upgrade

### Posit.Cli
- `PromptBuilder.cs` — stub usage rules (ReadLines not ReadFile)

### Posit.Phases/EdgeCases (NEW)
- `EdgeCaseCatalog.cs`, `InputValidationPatterns.cs`, `SqlInjectionPatterns.cs`
- `ConcurrencyPatterns.cs`, `ApiErrorPatterns.cs`

### patterns/cut-outs/
- `row-validator.dfy` — returns (outRows, isValid) not just ValidationResult

### wiki/trials/
- `trial-specs.md` — revamped with spike-based trials
- `trial-revamp-report.png` — 4-panel report chart

## Database
PostgreSQL 18 on port 5434. Database: shepherd. User: shepherd. Password: shepherd.
psql path: `C:\Program Files\PostgreSQL\16\bin\psql.exe`

## Models
- Architecture: deepseek-v4-flash:cloud (via Ollama, weekly limit reached)
- All other phases: deterministic (no model call)