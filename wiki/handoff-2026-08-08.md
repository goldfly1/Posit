---
title: "Posit Handoff Sheet — Aug 8, 2026"
type: handoff
tags: [posit, handoff, dafny, pipeline, trials]
component: all
version: 1.0.0
last_updated: 2026-08-08
---

# Posit Handoff Sheet — Aug 8, 2026

## Project

**Posit** — a spec compiler. The architect posits contracts (requires/ensures). Z3 confirms or denies. The code that survives is proven. Nothing ships unproven.

## Repos

- **Posit (greenfield):** `C:\Users\goldf\Posit\` — new project, Dafny-first pipeline
- **Shepherd (reference):** `C:\Users\goldf\orch\` — working pipeline, Dafny wiki, trial logs

## Current State

- Solution builds clean: 0 errors, 0 warnings.
- T1 (CSV parser) passed end-to-end with Docker verification and 87 tests.
- T2 (priority queue) passed end-to-end with Docker verification and 24 tests.
- T3 (TaskApi / ASP.NET Core CRUD + Postgres + Docker Compose) is in flight.
- `posit resume <session-id>` is implemented and verified.
- Blazor desktop UI (`Posit.Dt`) lists live sessions from Postgres.

## Recent Changes

| Change | File | Why |
|--------|------|-----|
| Docker verifier | `src/Posit.Tools/DockerVerifier.cs` | Materialize generated solution and run `dotnet build` + `dotnet test` in Docker |
| Test detection by xUnit attributes | `src/Posit.Tools/DockerVerifier.cs` | Project names are unreliable; `[Fact]`/`[Theory]` are truth |
| Duplicate project name dedup | `src/Posit.Tools/DockerVerifier.cs` | QA sometimes emits overlapping test directory names |
| Token caps to 64 K | `src/Posit.Cli/Orchestration/PositOrchestrator.cs`, `src/Posit.Phases/QaPhase.cs` | Avoid truncation on large Dafny / QA outputs |
| Correct model routing | `src/Posit.Cli/Orchestration/PositOrchestrator.cs` | `deepseek-v4-pro:cloud` for Dafny; `kimi-k2.7-code:cloud` for design review |
| C# phase no skeleton re-emission | `src/Posit.Phases/CSharpImplementationPhase.cs` | Collapse `*.Implementation`/`*Implementations`/`.extern` folders into parent module |
| C# phase no test/csproj emission | `src/Posit.Phases/CSharpImplementationPhase.cs`, `prompts/csharp-implementation/1.0.0.md` | QA owns tests; verifier owns `.csproj` |
| Dafny verification failure is real failure | `src/Posit.Phases/DafnyImplementationPhase.cs` | Pipeline was continuing on Z3 failure |
| `forall` needs `ensures` | `src/Posit.Phases/DafnyImplementationPhase.cs`, `prompts/architecture/1.0.0.md` | Fix `Repr`/`Valid()` anti-pattern |
| `frames` pattern | `patterns/frames.dfy` | Canonical `ghost predicate Valid()` + `Repr` for heap-owning classes |
| `posit resume` | `src/Posit.Cli/Program.cs`, `src/Posit.Cli/Orchestration/PositOrchestrator.cs` | Continue from last failed phase without re-burning tokens |
| `posit status` reads DB | `src/Posit.Cli/Program.cs` | No longer in-memory only |
| Async exception tests | `prompts/qa/1.0.0.md` | `await Assert.ThrowsAsync<T>` for async code |
| Blazor DT | `src/Posit.Dt/` | Live session listing UI |

## Verified Trials

### T1 — CSV Parser

- Session: `DgrruGRL1U-UzrQxS-nlsg0000`
- Result: Completed, 5 phases
- Docker build: 0 errors, 158 warnings
- Tests: 87 passed, 0 failed

### T2 — Priority Queue

- Session: `KgCMmAcgWE2YbFbfLBSzdg0000`
- Result: Completed, 5 phases
- Docker build: 0 errors, 87 warnings
- Tests: 24 passed, 0 failed

## Model Assignments

| Task | Model |
|------|-------|
| Architecture, Dafny Implementation | `deepseek-v4-pro:cloud` |
| Design Review, Imp Appeal | `kimi-k2.7-code:cloud` |
| C# Implementation, QA | `glm-5.2:cloud` |

## CLI Commands

```bash
# Run a trial
posit run "Build a CSV parser"

# Resume from a failed/killed session
posit resume <session-id>

# Check session status
posit status <session-id>

# List artifacts
posit artifacts <session-id>

# Verify in Docker
posit verify <session-id>
```

## Environment

- **Dafny:** 4.11.0 at `C:\Users\goldf\.dotnet\tools\dafny.exe`
- **Z3:** 4.12.1 at `C:\Users\goldf\.dotnet\tools\z3\bin\z3.exe`
- **Postgres:** `wiki-postgres` on 0.0.0.0:5435, `shepherd-postgres` on 127.0.0.1:5434
- **Docker:** 29.6.1
- **Target framework:** .NET 10 (`net10.0`)

## Carapace Principles (still in force)

1. The `.dfy` skeleton is the contract.
2. Patterns are objects cut from whole cloth, not partial classes to fill later.
3. Imp only inlays function into pre-cut `{:extern}` portals.
4. If a phase makes up names, the fix is contract enforcement, not more tolerant parsing.

## Next

- T3 (TaskApi) completion and Docker verification.
- Generate a structured Dafny language reference manual from the 2,126 Dafny examples in `orch/wiki/patterns/dafny-examples/` (on user request).
- Open Blazor DT for live session inspection (on user request).
