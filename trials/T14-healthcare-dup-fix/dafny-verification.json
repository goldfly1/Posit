[
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-PatientRegistry.dfy",
    "isVerified": true,
    "moduleName": "PatientRegistry",
    "dafnySource": "// Auto-composed skeleton for PatientRegistry\r\n// Pattern: repository\r\n// Stubs: none\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Repository (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Store items with ID uniqueness, lookup, and update\n// test: Add([], Record(1, \"test\", \"data\")) returns Success([Record(1, \"test\", \"data\")])\n// test: Add([Record(1, \"a\", \"b\")], Record(1, \"c\", \"d\")) returns Failure(\"duplicate id\")\n//\n// Parameters:\n//   idField: string \u2014 name of the ID field (for error messages)\n//   allowUpdate: bool \u2014 whether Add replaces existing IDs (default false)\n\ninclude \"result.dfy\"\n\ndatatype Entity =\n  | Record(id: int, name: string, data: string)\n\n// Invariant: no duplicate IDs\npredicate NoDuplicates(items: seq<Entity>)\n{\n  forall i, j :: 0 <= i < j < |items| ==> items[i].id != items[j].id\n}\n\n// Add: insert if ID not present, fail if duplicate\nmethod Add(items: seq<Entity>, entity: Entity) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| == |items| + 1\n  ensures result.Failure? ==> result.error == \"duplicate id\"\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    invariant NoDuplicates(items)\n    decreases |items| - i\n  {\n    if items[i].id == entity.id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    result := Failure(\"duplicate id\");\n  } else {\n    result := Success(items + [entity]);\n  }\n}\n\n// Find: lookup by ID\nmethod Find(items: seq<Entity>, id: int) returns (result: Result<Entity>)\n  ensures result.Success? ==> result.value.id == id\n  decreases |items|\n{\n  var i := 0;\n  while i < |items|\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      result := Success(items[i]);\n      return;\n    }\n    i := i + 1;\n  }\n  result := Failure(\"not found\");\n}\n\n// Remove: delete by ID\nmethod Remove(items: seq<Entity>, id: int) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| <= |items|\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    var newItems := [];\n    var j := 0;\n    while j < |items|\n      invariant 0 <= j <= |items|\n      invariant |newItems| <= j\n      decreases |items| - j\n    {\n      if items[j].id != id {\n        newItems := newItems + [items[j]];\n      }\n      j := j + 1;\n    }\n    result := Success(newItems);\n  } else {\n    result := Failure(\"not found\");\n  }\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": "Dafny program verifier finished with 7 verified, 0 errors",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-PatientRegistry.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-ClinicalRecords.dfy",
    "isVerified": true,
    "moduleName": "ClinicalRecords",
    "dafnySource": "// Auto-composed skeleton for ClinicalRecords\r\n// Pattern: repository\r\n// Stubs: console-io\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Repository (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Store items with ID uniqueness, lookup, and update\n// test: Add([], Record(1, \"test\", \"data\")) returns Success([Record(1, \"test\", \"data\")])\n// test: Add([Record(1, \"a\", \"b\")], Record(1, \"c\", \"d\")) returns Failure(\"duplicate id\")\n//\n// Parameters:\n//   idField: string \u2014 name of the ID field (for error messages)\n//   allowUpdate: bool \u2014 whether Add replaces existing IDs (default false)\n\ninclude \"result.dfy\"\n\ndatatype Entity =\n  | Record(id: int, name: string, data: string)\n\n// Invariant: no duplicate IDs\npredicate NoDuplicates(items: seq<Entity>)\n{\n  forall i, j :: 0 <= i < j < |items| ==> items[i].id != items[j].id\n}\n\n// Add: insert if ID not present, fail if duplicate\nmethod Add(items: seq<Entity>, entity: Entity) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| == |items| + 1\n  ensures result.Failure? ==> result.error == \"duplicate id\"\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    invariant NoDuplicates(items)\n    decreases |items| - i\n  {\n    if items[i].id == entity.id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    result := Failure(\"duplicate id\");\n  } else {\n    result := Success(items + [entity]);\n  }\n}\n\n// Find: lookup by ID\nmethod Find(items: seq<Entity>, id: int) returns (result: Result<Entity>)\n  ensures result.Success? ==> result.value.id == id\n  decreases |items|\n{\n  var i := 0;\n  while i < |items|\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      result := Success(items[i]);\n      return;\n    }\n    i := i + 1;\n  }\n  result := Failure(\"not found\");\n}\n\n// Remove: delete by ID\nmethod Remove(items: seq<Entity>, id: int) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| <= |items|\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    var newItems := [];\n    var j := 0;\n    while j < |items|\n      invariant 0 <= j <= |items|\n      invariant |newItems| <= j\n      decreases |items| - j\n    {\n      if items[j].id != id {\n        newItems := newItems + [items[j]];\n      }\n      j := j + 1;\n    }\n    result := Success(newItems);\n  } else {\n    result := Failure(\"not found\");\n  }\n}\r\n\r\n// ---- console-io portals ----\r\n// Stubs: Console I/O portals\n// Pre-cut {:extern} declarations for console operations.\n// Attach to patterns that interact with the user.\n\n// Print a line to console\nmethod {:extern} PrintLine(text: string)\n  ensures true\n\n// Print without newline\nmethod {:extern} Print(text: string)\n  ensures true\n\n// Read a line from console\nmethod {:extern} ReadLine() returns (line: string)\n  ensures |line| >= 0\n\n// Read a character from console\nmethod {:extern} ReadChar() returns (ch: string)\n  ensures |ch| <= 1\n\n// Clear the console\nmethod {:extern} ClearScreen()\n  ensures true\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-ClinicalRecords.dfy(116,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n116 | method {:extern} PrintLine(text: string)\r\n    |                  ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(120,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n120 | method {:extern} Print(text: string)\r\n    |                  ^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(124,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n124 | method {:extern} ReadLine() returns (line: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(128,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n128 | method {:extern} ReadChar() returns (ch: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(132,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n132 | method {:extern} ClearScreen()\r\n    |                  ^^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 7 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-ClinicalRecords.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AppointmentScheduler.dfy",
    "isVerified": true,
    "moduleName": "AppointmentScheduler",
    "dafnySource": "// Auto-composed skeleton for AppointmentScheduler\r\n// Pattern: scheduler\r\n// Stubs: none\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Scheduler (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Enqueue, dequeue, and prioritize tasks\n// test: Dequeue([Entry(Task(1, \"a\"), 5), Entry(Task(2, \"b\"), 10)]) returns Success(Entry(Task(2, \"b\"), 10))\n// test: Dequeue([Entry(Task(1, \"a\"), 5)]) returns Success(Entry(Task(1, \"a\"), 5))\n// test: Peek([Entry(Task(1, \"a\"), 5), Entry(Task(2, \"b\"), 10)]) returns Success(Entry(Task(2, \"b\"), 10))\n// test: Prioritize([Entry(Task(1, \"a\"), 5), Entry(Task(2, \"b\"), 10)]) returns [Entry(Task(2, \"b\"), 10), Entry(Task(1, \"a\"), 5)]\n//\n// Parameters:\n//   policy: string \u2014 \"priority\" (highest first) or \"fifo\" (first in first out)\n//   maxQueueSize: int \u2014 maximum queue depth (0 = unlimited)\n\ninclude \"result.dfy\"\n\ndatatype Task = Task(id: int, name: string)\ndatatype Entry = Entry(task: Task, priority: int)\n\n// Predicate: sequence is sorted in descending priority order\npredicate IsSortedDesc(s: seq<Entry>)\n{\n  forall i :: 0 <= i < |s| - 1 ==> s[i].priority >= s[i + 1].priority\n}\n\n// Enqueue: add an entry to the end of the queue\nmethod Enqueue(queue: seq<Entry>, entry: Entry) returns (newQueue: seq<Entry>)\n  ensures |newQueue| == |queue| + 1\n  ensures newQueue[|queue|] == entry\n  ensures forall i :: 0 <= i < |queue| ==> newQueue[i] == queue[i]\n{\n  newQueue := queue + [entry];\n}\n\n// Dequeue: remove and return the highest-priority entry\nmethod Dequeue(queue: seq<Entry>) returns (result: Result<Entry>, newQueue: seq<Entry>)\n  requires |queue| > 0\n  ensures result.Success?\n  ensures |newQueue| == |queue| - 1\n  ensures forall j :: 0 <= j < |queue| ==> result.value.priority >= queue[j].priority\n  ensures result.value in queue\n  decreases |queue|\n{\n  var maxIdx := 0;\n  var i := 1;\n  while i < |queue|\n    invariant 0 <= maxIdx < i <= |queue|\n    invariant forall j :: 0 <= j < i ==> queue[j].priority <= queue[maxIdx].priority\n    decreases |queue| - i\n  {\n    if queue[i].priority > queue[maxIdx].priority {\n      maxIdx := i;\n    }\n    i := i + 1;\n  }\n  result := Success(queue[maxIdx]);\n  newQueue := queue[..maxIdx] + queue[maxIdx + 1..];\n}\n\n// Peek: return the highest-priority entry without removing it\nmethod Peek(queue: seq<Entry>) returns (result: Result<Entry>)\n  requires |queue| > 0\n  ensures result.Success?\n  ensures forall j :: 0 <= j < |queue| ==> result.value.priority >= queue[j].priority\n  ensures result.value in queue\n  decreases |queue|\n{\n  var maxIdx := 0;\n  var i := 1;\n  while i < |queue|\n    invariant 0 <= maxIdx < i <= |queue|\n    invariant forall j :: 0 <= j < i ==> queue[j].priority <= queue[maxIdx].priority\n    decreases |queue| - i\n  {\n    if queue[i].priority > queue[maxIdx].priority {\n      maxIdx := i;\n    }\n    i := i + 1;\n  }\n  result := Success(queue[maxIdx]);\n}\n\n// Insert an entry into a sorted queue, maintaining descending priority order\nfunction InsertSorted(sorted: seq<Entry>, e: Entry): seq<Entry>\n  requires IsSortedDesc(sorted)\n  ensures |InsertSorted(sorted, e)| == |sorted| + 1\n  ensures IsSortedDesc(InsertSorted(sorted, e))\n  decreases |sorted|\n{\n  if |sorted| == 0 then [e]\n  else if e.priority >= sorted[0].priority then [e] + sorted\n  else [sorted[0]] + InsertSorted(sorted[1..], e)\n}\n\n// Prioritize: sort the queue by priority (highest first) using insertion sort\nmethod Prioritize(queue: seq<Entry>) returns (sorted: seq<Entry>)\n  ensures |sorted| == |queue|\n  ensures IsSortedDesc(sorted)\n  decreases |queue|\n{\n  sorted := [];\n  var i := 0;\n  while i < |queue|\n    invariant 0 <= i <= |queue|\n    invariant |sorted| == i\n    invariant IsSortedDesc(sorted)\n    decreases |queue| - i\n  {\n    sorted := InsertSorted(sorted, queue[i]);\n    i := i + 1;\n  }\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": "Dafny program verifier finished with 9 verified, 0 errors",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AppointmentScheduler.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-BillingManager.dfy",
    "isVerified": true,
    "moduleName": "BillingManager",
    "dafnySource": "// Auto-composed skeleton for BillingManager\r\n// Pattern: state-machine\r\n// Stubs: none\r\n\r\n// Pattern: State Machine (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Finite states with guarded transitions\n// test: Transition(Idle, \"start\") returns Active\n// test: Transition(Active, \"complete\") returns Completed\n// test: Transition(Idle, \"complete\") returns Idle (invalid transition)\n//\n// Parameters:\n//   states: the datatype variants (customized per domain)\n//   events: the event strings (customized per domain)\n//   guards: which transitions are valid (the match body)\n\ndatatype State =\n  | Idle\n  | Active\n  | Completed\n  | Failed\n\n// Transition: state x event -> state\n// Invalid transitions stay in current state\nmethod Transition(current: State, event: string) returns (next: State)\n  ensures next == Idle || next == Active || next == Completed || next == Failed\n{\n  match current\n  case Idle =>\n    if event == \"start\" {\n      next := Active;\n    } else {\n      next := Idle;\n    }\n  case Active =>\n    if event == \"complete\" {\n      next := Completed;\n    } else if event == \"fail\" {\n      next := Failed;\n    } else {\n      next := Active;\n    }\n  case Completed =>\n    if event == \"reset\" {\n      next := Idle;\n    } else {\n      next := Completed;\n    }\n  case Failed =>\n    if event == \"reset\" {\n      next := Idle;\n    } else {\n      next := Failed;\n    }\n}\n\n// Check if a transition is valid without executing it\nfunction IsValidTransition(current: State, event: string): bool\n{\n  match current\n  case Idle => event == \"start\"\n  case Active => event == \"complete\" || event == \"fail\"\n  case Completed => event == \"reset\"\n  case Failed => event == \"reset\"\n}\n\n// Get all valid events for a state\nmethod GetValidEvents(current: State) returns (events: seq<string>)\n  ensures |events| >= 0\n{\n  if current == Idle {\n    events := [\"start\"];\n  } else if current == Active {\n    events := [\"complete\", \"fail\"];\n  } else if current == Completed {\n    events := [\"reset\"];\n  } else {\n    events := [\"reset\"];\n  }\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": "Dafny program verifier finished with 2 verified, 0 errors",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-BillingManager.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-FhirModels.dfy",
    "isVerified": true,
    "moduleName": "FhirModels",
    "dafnySource": "// Auto-composed skeleton for FhirModels\r\n// Pattern: transformer\r\n// Stubs: console-io\r\n\r\n// Pattern: Transformer (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Transform input through stages to output\n// test: ToUpper([\"hello\", \"world\"]) returns [\"HELLO\", \"WORLD\"]\n// test: Trim([\" a \", \" b \"]) returns [\"a\", \"b\"]\n//\n// Parameters:\n//   operation: the transformation applied per element (ToUpper, ToLower, Trim, etc.)\n//   filterEmpty: bool \u2014 whether to drop empty results (default false)\n\n// Apply a transformation to each element of a sequence\n// SUBSTITUTION_MARKER: operation=ToUpper (replaced by generate-batch.py)\nmethod TransformAll(input: seq<string>) returns (output: seq<string>)\n  requires |input| > 0\n  ensures |output| == |input|\n  decreases |input|\n{\n  output := [];\n  var i := 0;\n  while i < |input|\n    invariant 0 <= i <= |input|\n    invariant |output| == i\n    decreases |input| - i\n  {\n    output := output + [ToUpper(input[i])];\n    i := i + 1;\n  }\n}\n\n// Transform and filter out empty results\nmethod TransformAndFilter(input: seq<string>) returns (output: seq<string>)\n  requires |input| > 0\n  ensures |output| >= 0\n  ensures |output| <= |input|\n  decreases |input|\n{\n  output := [];\n  var i := 0;\n  while i < |input|\n    invariant 0 <= i <= |input|\n    invariant |output| <= i\n    decreases |input| - i\n  {\n    var transformed := ToUpper(input[i]);\n    if |transformed| > 0 {\n      output := output + [transformed];\n    }\n    i := i + 1;\n  }\n}\n\n// Convert string to uppercase\nfunction ToUpper(s: string): string\n  decreases |s|\n{\n  if |s| == 0 then \"\"\n  else if 'a' <= s[0] && s[0] <= 'z' then [CharToUpper(s[0])] + ToUpper(s[1..])\n  else [s[0]] + ToUpper(s[1..])\n}\n\n// Convert lowercase char to uppercase\nfunction CharToUpper(c: char): char\n  ensures 'a' <= c <= 'z' ==> 'A' <= CharToUpper(c) <= 'Z'\n{\n  match c\n  case 'a' => 'A'\n  case 'b' => 'B'\n  case 'c' => 'C'\n  case 'd' => 'D'\n  case 'e' => 'E'\n  case 'f' => 'F'\n  case 'g' => 'G'\n  case 'h' => 'H'\n  case 'i' => 'I'\n  case 'j' => 'J'\n  case 'k' => 'K'\n  case 'l' => 'L'\n  case 'm' => 'M'\n  case 'n' => 'N'\n  case 'o' => 'O'\n  case 'p' => 'P'\n  case 'q' => 'Q'\n  case 'r' => 'R'\n  case 's' => 'S'\n  case 't' => 'T'\n  case 'u' => 'U'\n  case 'v' => 'V'\n  case 'w' => 'W'\n  case 'x' => 'X'\n  case 'y' => 'Y'\n  case 'z' => 'Z'\n  case _ => c\n}\n\n// Reverse a string\nfunction Reverse(s: string): string\n  ensures |Reverse(s)| == |s|\n  decreases |s|\n{\n  if |s| == 0 then \"\"\n  else Reverse(s[1..]) + [s[0]]\n}\r\n\r\n// ---- console-io portals ----\r\n// Stubs: Console I/O portals\n// Pre-cut {:extern} declarations for console operations.\n// Attach to patterns that interact with the user.\n\n// Print a line to console\nmethod {:extern} PrintLine(text: string)\n  ensures true\n\n// Print without newline\nmethod {:extern} Print(text: string)\n  ensures true\n\n// Read a line from console\nmethod {:extern} ReadLine() returns (line: string)\n  ensures |line| >= 0\n\n// Read a character from console\nmethod {:extern} ReadChar() returns (ch: string)\n  ensures |ch| <= 1\n\n// Clear the console\nmethod {:extern} ClearScreen()\n  ensures true\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-FhirModels.dfy(113,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n113 | method {:extern} PrintLine(text: string)\r\n    |                  ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(117,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n117 | method {:extern} Print(text: string)\r\n    |                  ^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(121,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n121 | method {:extern} ReadLine() returns (line: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(125,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n125 | method {:extern} ReadChar() returns (ch: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(129,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n129 | method {:extern} ClearScreen()\r\n    |                  ^^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 5 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-FhirModels.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AuditTrail.dfy",
    "isVerified": true,
    "moduleName": "AuditTrail",
    "dafnySource": "// Auto-composed skeleton for AuditTrail\r\n// Pattern: repository\r\n// Stubs: time-random\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Repository (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Store items with ID uniqueness, lookup, and update\n// test: Add([], Record(1, \"test\", \"data\")) returns Success([Record(1, \"test\", \"data\")])\n// test: Add([Record(1, \"a\", \"b\")], Record(1, \"c\", \"d\")) returns Failure(\"duplicate id\")\n//\n// Parameters:\n//   idField: string \u2014 name of the ID field (for error messages)\n//   allowUpdate: bool \u2014 whether Add replaces existing IDs (default false)\n\ninclude \"result.dfy\"\n\ndatatype Entity =\n  | Record(id: int, name: string, data: string)\n\n// Invariant: no duplicate IDs\npredicate NoDuplicates(items: seq<Entity>)\n{\n  forall i, j :: 0 <= i < j < |items| ==> items[i].id != items[j].id\n}\n\n// Add: insert if ID not present, fail if duplicate\nmethod Add(items: seq<Entity>, entity: Entity) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| == |items| + 1\n  ensures result.Failure? ==> result.error == \"duplicate id\"\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    invariant NoDuplicates(items)\n    decreases |items| - i\n  {\n    if items[i].id == entity.id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    result := Failure(\"duplicate id\");\n  } else {\n    result := Success(items + [entity]);\n  }\n}\n\n// Find: lookup by ID\nmethod Find(items: seq<Entity>, id: int) returns (result: Result<Entity>)\n  ensures result.Success? ==> result.value.id == id\n  decreases |items|\n{\n  var i := 0;\n  while i < |items|\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      result := Success(items[i]);\n      return;\n    }\n    i := i + 1;\n  }\n  result := Failure(\"not found\");\n}\n\n// Remove: delete by ID\nmethod Remove(items: seq<Entity>, id: int) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| <= |items|\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    var newItems := [];\n    var j := 0;\n    while j < |items|\n      invariant 0 <= j <= |items|\n      invariant |newItems| <= j\n      decreases |items| - j\n    {\n      if items[j].id != id {\n        newItems := newItems + [items[j]];\n      }\n      j := j + 1;\n    }\n    result := Success(newItems);\n  } else {\n    result := Failure(\"not found\");\n  }\n}\r\n\r\n// ---- time-random portals ----\r\n// Stubs: Time and Random portals\n// Pre-cut {:extern} declarations for non-deterministic operations.\n// Attach to patterns that need timestamps, delays, or randomness.\n\n// Get current time as Unix timestamp (seconds since epoch)\nmethod {:extern} GetCurrentTime() returns (timestamp: int)\n  ensures timestamp > 0\n\n// Get current time as ISO string\nmethod {:extern} GetIsoTimestamp() returns (timestamp: string)\n  ensures |timestamp| > 0\n\n// Sleep for N milliseconds\nmethod {:extern} Sleep(milliseconds: int)\n  requires milliseconds >= 0\n\n// Get a random integer in range [min, max)\nmethod {:extern} RandomInt(min: int, max: int) returns (value: int)\n  requires min < max\n  ensures min <= value < max\n\n// Get a random boolean\nmethod {:extern} RandomBool() returns (value: bool)\n  ensures true\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-AuditTrail.dfy(116,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n116 | method {:extern} GetCurrentTime() returns (timestamp: int)\r\n    |                  ^^^^^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-AuditTrail.dfy(120,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n120 | method {:extern} GetIsoTimestamp() returns (timestamp: string)\r\n    |                  ^^^^^^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-AuditTrail.dfy(128,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n128 | method {:extern} RandomInt(min: int, max: int) returns (value: int)\r\n    |                  ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-AuditTrail.dfy(133,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n133 | method {:extern} RandomBool() returns (value: bool)\r\n    |                  ^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 7 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AuditTrail.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-IntegrationGateway.dfy",
    "isVerified": true,
    "moduleName": "IntegrationGateway",
    "dafnySource": "// Auto-composed skeleton for IntegrationGateway\r\n// Pattern: adapter\r\n// Stubs: network-io\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Adapter (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Wrap an external API in a clean interface\n// test: SendRequest(SimpleRequest(\"GET\", \"/users\")) returns Response(200, \"[]\")\n// test: SendRequest(FullRequest(\"POST\", \"/users\", \"data\")) returns Response(201, \"created\")\n//\n// Parameters:\n//   baseUrl: string \u2014 the base URL for the external API (default \"https://api.example.com\")\n//   timeoutMs: int \u2014 request timeout in milliseconds (default 5000)\n//   defaultHeaders: seq<string> \u2014 headers added to every request (default [])\n\ninclude \"result.dfy\"\n\ndatatype Request =\n  | SimpleRequest(verb: string, path: string)\n  | FullRequest(verb: string, path: string, body: string)\n\ndatatype Response =\n  | Response(status: int, body: string)\n\n// Send a single request through the adapter\nmethod SendRequest(req: Request) returns (resp: Response)\n  ensures resp.Response?\n{\n  if req.SimpleRequest? {\n    if req.verb == \"GET\" {\n      resp := Response(200, \"[]\");\n    } else {\n      resp := Response(200, \"ok\");\n    }\n  } else if req.FullRequest? {\n    if req.verb == \"POST\" {\n      resp := Response(201, \"created\");\n    } else if req.verb == \"PUT\" {\n      resp := Response(200, \"updated\");\n    } else if req.verb == \"DELETE\" {\n      resp := Response(204, \"\");\n    } else {\n      resp := Response(400, \"bad method\");\n    }\n  } else {\n    resp := Response(400, \"unknown request type\");\n  }\n}\n\n// Send multiple requests in batch, collecting responses\nmethod BatchSend(requests: seq<Request>) returns (responses: seq<Response>)\n  requires |requests| > 0\n  ensures |responses| == |requests|\n  decreases |requests|\n{\n  responses := [];\n  var i := 0;\n  while i < |requests|\n    invariant 0 <= i <= |requests|\n    invariant |responses| == i\n    decreases |requests| - i\n  {\n    var r := SendRequest(requests[i]);\n    responses := responses + [r];\n    i := i + 1;\n  }\n}\n\n// Send with retry: attempts the request up to maxAttempts times\nmethod SendWithRetry(req: Request, maxAttempts: int) returns (result: Result<Response>)\n  requires maxAttempts > 0\n  ensures result.Success? || result.Failure?\n  ensures result.Success? ==> result.value.Response?\n  ensures result.Failure? ==> result.error == \"all attempts failed\"\n  decreases maxAttempts\n{\n  var attempts := 0;\n  result := Failure(\"all attempts failed\");\n  while attempts < maxAttempts\n    invariant 0 <= attempts <= maxAttempts\n    invariant result.Failure? ==> result.error == \"all attempts failed\"\n    decreases maxAttempts - attempts\n  {\n    var resp := SendRequest(req);\n    if resp.status >= 200 && resp.status < 300 {\n      result := Success(resp);\n      return;\n    }\n    attempts := attempts + 1;\n  }\n}\n\n// Validate a request before sending\nmethod ValidateRequest(req: Request) returns (result: Result<Request>)\n  ensures result.Success? ==> result.value == req\n  ensures result.Failure? ==> |result.error| > 0\n{\n  if req.SimpleRequest? {\n    if |req.verb| == 0 {\n      result := Failure(\"method is empty\");\n    } else if |req.path| == 0 {\n      result := Failure(\"path is empty\");\n    } else if req.path[0] != '/' {\n      result := Failure(\"path must start with /\");\n    } else {\n      result := Success(req);\n    }\n  } else if req.FullRequest? {\n    if |req.verb| == 0 {\n      result := Failure(\"method is empty\");\n    } else if |req.path| == 0 {\n      result := Failure(\"path is empty\");\n    } else if req.path[0] != '/' {\n      result := Failure(\"path must start with /\");\n    } else {\n      result := Success(req);\n    }\n  } else {\n    result := Failure(\"unknown request type\");\n  }\n}\n\n// Count successful responses (status 200-299) in a batch\nmethod CountSuccessful(responses: seq<Response>) returns (count: int)\n  ensures count >= 0\n  ensures count <= |responses|\n  decreases |responses|\n{\n  count := 0;\n  var i := 0;\n  while i < |responses|\n    invariant 0 <= i <= |responses|\n    invariant count <= i\n    decreases |responses| - i\n  {\n    if responses[i].status >= 200 && responses[i].status < 300 {\n      count := count + 1;\n    }\n    i := i + 1;\n  }\n}\r\n\r\n// ---- network-io portals ----\r\n// Stubs: Network I/O portals\n// Pre-cut {:extern} declarations for HTTP and network operations.\n// Attach to patterns that make web requests or API calls.\n\n// HTTP GET request\nmethod {:extern} HttpGet(url: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// HTTP POST request\nmethod {:extern} HttpPost(url: string, body: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// HTTP PUT request\nmethod {:extern} HttpPut(url: string, body: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// HTTP DELETE request\nmethod {:extern} HttpDelete(url: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// Open a TCP connection\nmethod {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\n  requires |host| > 0\n  requires port > 0\n  ensures socketId >= 0\n\n// Send data over socket\nmethod {:extern} SocketSend(socketId: int, data: string)\n  requires socketId >= 0\n\n// Receive data from socket\nmethod {:extern} SocketRecv(socketId: int) returns (data: string)\n  requires socketId >= 0\n  ensures |data| >= 0\n\n// Close socket\nmethod {:extern} CloseSocket(socketId: int)\n  requires socketId >= 0\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-IntegrationGateway.dfy(150,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n150 | method {:extern} HttpGet(url: string) returns (response: string)\r\n    |                  ^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(155,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n155 | method {:extern} HttpPost(url: string, body: string) returns (response: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(160,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n160 | method {:extern} HttpPut(url: string, body: string) returns (response: string)\r\n    |                  ^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(165,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n165 | method {:extern} HttpDelete(url: string) returns (response: string)\r\n    |                  ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(170,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n170 | method {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\r\n    |                  ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(180,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n180 | method {:extern} SocketRecv(socketId: int) returns (data: string)\r\n    |                  ^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 7 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-IntegrationGateway.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-WebApi.dfy",
    "isVerified": true,
    "moduleName": "WebApi",
    "dafnySource": "// Auto-composed io-shell skeleton for WebApi\r\n// Stubs: network-io, console-io\r\n\r\nmodule WebApi {\r\n\r\n  // ---- network-io portals ----\r\n  // Stubs: Network I/O portals\r\n  // Pre-cut {:extern} declarations for HTTP and network operations.\r\n  // Attach to patterns that make web requests or API calls.\r\n\r\n  // HTTP GET request\r\n  method {:extern} HttpGet(url: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // HTTP POST request\r\n  method {:extern} HttpPost(url: string, body: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // HTTP PUT request\r\n  method {:extern} HttpPut(url: string, body: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // HTTP DELETE request\r\n  method {:extern} HttpDelete(url: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // Open a TCP connection\r\n  method {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\r\n    requires |host| > 0\r\n    requires port > 0\r\n    ensures socketId >= 0\r\n\r\n  // Send data over socket\r\n  method {:extern} SocketSend(socketId: int, data: string)\r\n    requires socketId >= 0\r\n\r\n  // Receive data from socket\r\n  method {:extern} SocketRecv(socketId: int) returns (data: string)\r\n    requires socketId >= 0\r\n    ensures |data| >= 0\r\n\r\n  // Close socket\r\n  method {:extern} CloseSocket(socketId: int)\r\n    requires socketId >= 0\r\n\r\n  // ---- console-io portals ----\r\n  // Stubs: Console I/O portals\r\n  // Pre-cut {:extern} declarations for console operations.\r\n  // Attach to patterns that interact with the user.\r\n\r\n  // Print a line to console\r\n  method {:extern} PrintLine(text: string)\r\n    ensures true\r\n\r\n  // Print without newline\r\n  method {:extern} Print(text: string)\r\n    ensures true\r\n\r\n  // Read a line from console\r\n  method {:extern} ReadLine() returns (line: string)\r\n    ensures |line| >= 0\r\n\r\n  // Read a character from console\r\n  method {:extern} ReadChar() returns (ch: string)\r\n    ensures |ch| <= 1\r\n\r\n  // Clear the console\r\n  method {:extern} ClearScreen()\r\n    ensures true\r\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-WebApi.dfy(12,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n12 |   method {:extern} HttpGet(url: string) returns (response: string)\r\n   |                    ^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(17,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n17 |   method {:extern} HttpPost(url: string, body: string) returns (response: string)\r\n   |                    ^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(22,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n22 |   method {:extern} HttpPut(url: string, body: string) returns (response: string)\r\n   |                    ^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(27,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n27 |   method {:extern} HttpDelete(url: string) returns (response: string)\r\n   |                    ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(32,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n32 |   method {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\r\n   |                    ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(42,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n42 |   method {:extern} SocketRecv(socketId: int) returns (data: string)\r\n   |                    ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(56,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n56 |   method {:extern} PrintLine(text: string)\r\n   |                    ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(60,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n60 |   method {:extern} Print(text: string)\r\n   |                    ^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(64,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n64 |   method {:extern} ReadLine() returns (line: string)\r\n   |                    ^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(68,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n68 |   method {:extern} ReadChar() returns (ch: string)\r\n   |                    ^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(72,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n72 |   method {:extern} ClearScreen()\r\n   |                    ^^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 0 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-WebApi.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-PatientRegistry.dfy",
    "isVerified": true,
    "moduleName": "PatientRegistry",
    "dafnySource": "// Auto-composed skeleton for PatientRegistry\r\n// Pattern: repository\r\n// Stubs: none\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Repository (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Store items with ID uniqueness, lookup, and update\n// test: Add([], Record(1, \"test\", \"data\")) returns Success([Record(1, \"test\", \"data\")])\n// test: Add([Record(1, \"a\", \"b\")], Record(1, \"c\", \"d\")) returns Failure(\"duplicate id\")\n//\n// Parameters:\n//   idField: string \u2014 name of the ID field (for error messages)\n//   allowUpdate: bool \u2014 whether Add replaces existing IDs (default false)\n\ninclude \"result.dfy\"\n\ndatatype Entity =\n  | Record(id: int, name: string, data: string)\n\n// Invariant: no duplicate IDs\npredicate NoDuplicates(items: seq<Entity>)\n{\n  forall i, j :: 0 <= i < j < |items| ==> items[i].id != items[j].id\n}\n\n// Add: insert if ID not present, fail if duplicate\nmethod Add(items: seq<Entity>, entity: Entity) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| == |items| + 1\n  ensures result.Failure? ==> result.error == \"duplicate id\"\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    invariant NoDuplicates(items)\n    decreases |items| - i\n  {\n    if items[i].id == entity.id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    result := Failure(\"duplicate id\");\n  } else {\n    result := Success(items + [entity]);\n  }\n}\n\n// Find: lookup by ID\nmethod Find(items: seq<Entity>, id: int) returns (result: Result<Entity>)\n  ensures result.Success? ==> result.value.id == id\n  decreases |items|\n{\n  var i := 0;\n  while i < |items|\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      result := Success(items[i]);\n      return;\n    }\n    i := i + 1;\n  }\n  result := Failure(\"not found\");\n}\n\n// Remove: delete by ID\nmethod Remove(items: seq<Entity>, id: int) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| <= |items|\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    var newItems := [];\n    var j := 0;\n    while j < |items|\n      invariant 0 <= j <= |items|\n      invariant |newItems| <= j\n      decreases |items| - j\n    {\n      if items[j].id != id {\n        newItems := newItems + [items[j]];\n      }\n      j := j + 1;\n    }\n    result := Success(newItems);\n  } else {\n    result := Failure(\"not found\");\n  }\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": "Dafny program verifier finished with 7 verified, 0 errors",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-PatientRegistry.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-ClinicalRecords.dfy",
    "isVerified": true,
    "moduleName": "ClinicalRecords",
    "dafnySource": "// Auto-composed skeleton for ClinicalRecords\r\n// Pattern: repository\r\n// Stubs: console-io\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Repository (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Store items with ID uniqueness, lookup, and update\n// test: Add([], Record(1, \"test\", \"data\")) returns Success([Record(1, \"test\", \"data\")])\n// test: Add([Record(1, \"a\", \"b\")], Record(1, \"c\", \"d\")) returns Failure(\"duplicate id\")\n//\n// Parameters:\n//   idField: string \u2014 name of the ID field (for error messages)\n//   allowUpdate: bool \u2014 whether Add replaces existing IDs (default false)\n\ninclude \"result.dfy\"\n\ndatatype Entity =\n  | Record(id: int, name: string, data: string)\n\n// Invariant: no duplicate IDs\npredicate NoDuplicates(items: seq<Entity>)\n{\n  forall i, j :: 0 <= i < j < |items| ==> items[i].id != items[j].id\n}\n\n// Add: insert if ID not present, fail if duplicate\nmethod Add(items: seq<Entity>, entity: Entity) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| == |items| + 1\n  ensures result.Failure? ==> result.error == \"duplicate id\"\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    invariant NoDuplicates(items)\n    decreases |items| - i\n  {\n    if items[i].id == entity.id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    result := Failure(\"duplicate id\");\n  } else {\n    result := Success(items + [entity]);\n  }\n}\n\n// Find: lookup by ID\nmethod Find(items: seq<Entity>, id: int) returns (result: Result<Entity>)\n  ensures result.Success? ==> result.value.id == id\n  decreases |items|\n{\n  var i := 0;\n  while i < |items|\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      result := Success(items[i]);\n      return;\n    }\n    i := i + 1;\n  }\n  result := Failure(\"not found\");\n}\n\n// Remove: delete by ID\nmethod Remove(items: seq<Entity>, id: int) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| <= |items|\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    var newItems := [];\n    var j := 0;\n    while j < |items|\n      invariant 0 <= j <= |items|\n      invariant |newItems| <= j\n      decreases |items| - j\n    {\n      if items[j].id != id {\n        newItems := newItems + [items[j]];\n      }\n      j := j + 1;\n    }\n    result := Success(newItems);\n  } else {\n    result := Failure(\"not found\");\n  }\n}\r\n\r\n// ---- console-io portals ----\r\n// Stubs: Console I/O portals\n// Pre-cut {:extern} declarations for console operations.\n// Attach to patterns that interact with the user.\n\n// Print a line to console\nmethod {:extern} PrintLine(text: string)\n  ensures true\n\n// Print without newline\nmethod {:extern} Print(text: string)\n  ensures true\n\n// Read a line from console\nmethod {:extern} ReadLine() returns (line: string)\n  ensures |line| >= 0\n\n// Read a character from console\nmethod {:extern} ReadChar() returns (ch: string)\n  ensures |ch| <= 1\n\n// Clear the console\nmethod {:extern} ClearScreen()\n  ensures true\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-ClinicalRecords.dfy(116,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n116 | method {:extern} PrintLine(text: string)\r\n    |                  ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(120,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n120 | method {:extern} Print(text: string)\r\n    |                  ^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(124,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n124 | method {:extern} ReadLine() returns (line: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(128,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n128 | method {:extern} ReadChar() returns (ch: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-ClinicalRecords.dfy(132,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n132 | method {:extern} ClearScreen()\r\n    |                  ^^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 7 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-ClinicalRecords.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AppointmentScheduler.dfy",
    "isVerified": true,
    "moduleName": "AppointmentScheduler",
    "dafnySource": "// Auto-composed skeleton for AppointmentScheduler\r\n// Pattern: scheduler\r\n// Stubs: none\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Scheduler (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Enqueue, dequeue, and prioritize tasks\n// test: Dequeue([Entry(Task(1, \"a\"), 5), Entry(Task(2, \"b\"), 10)]) returns Success(Entry(Task(2, \"b\"), 10))\n// test: Dequeue([Entry(Task(1, \"a\"), 5)]) returns Success(Entry(Task(1, \"a\"), 5))\n// test: Peek([Entry(Task(1, \"a\"), 5), Entry(Task(2, \"b\"), 10)]) returns Success(Entry(Task(2, \"b\"), 10))\n// test: Prioritize([Entry(Task(1, \"a\"), 5), Entry(Task(2, \"b\"), 10)]) returns [Entry(Task(2, \"b\"), 10), Entry(Task(1, \"a\"), 5)]\n//\n// Parameters:\n//   policy: string \u2014 \"priority\" (highest first) or \"fifo\" (first in first out)\n//   maxQueueSize: int \u2014 maximum queue depth (0 = unlimited)\n\ninclude \"result.dfy\"\n\ndatatype Task = Task(id: int, name: string)\ndatatype Entry = Entry(task: Task, priority: int)\n\n// Predicate: sequence is sorted in descending priority order\npredicate IsSortedDesc(s: seq<Entry>)\n{\n  forall i :: 0 <= i < |s| - 1 ==> s[i].priority >= s[i + 1].priority\n}\n\n// Enqueue: add an entry to the end of the queue\nmethod Enqueue(queue: seq<Entry>, entry: Entry) returns (newQueue: seq<Entry>)\n  ensures |newQueue| == |queue| + 1\n  ensures newQueue[|queue|] == entry\n  ensures forall i :: 0 <= i < |queue| ==> newQueue[i] == queue[i]\n{\n  newQueue := queue + [entry];\n}\n\n// Dequeue: remove and return the highest-priority entry\nmethod Dequeue(queue: seq<Entry>) returns (result: Result<Entry>, newQueue: seq<Entry>)\n  requires |queue| > 0\n  ensures result.Success?\n  ensures |newQueue| == |queue| - 1\n  ensures forall j :: 0 <= j < |queue| ==> result.value.priority >= queue[j].priority\n  ensures result.value in queue\n  decreases |queue|\n{\n  var maxIdx := 0;\n  var i := 1;\n  while i < |queue|\n    invariant 0 <= maxIdx < i <= |queue|\n    invariant forall j :: 0 <= j < i ==> queue[j].priority <= queue[maxIdx].priority\n    decreases |queue| - i\n  {\n    if queue[i].priority > queue[maxIdx].priority {\n      maxIdx := i;\n    }\n    i := i + 1;\n  }\n  result := Success(queue[maxIdx]);\n  newQueue := queue[..maxIdx] + queue[maxIdx + 1..];\n}\n\n// Peek: return the highest-priority entry without removing it\nmethod Peek(queue: seq<Entry>) returns (result: Result<Entry>)\n  requires |queue| > 0\n  ensures result.Success?\n  ensures forall j :: 0 <= j < |queue| ==> result.value.priority >= queue[j].priority\n  ensures result.value in queue\n  decreases |queue|\n{\n  var maxIdx := 0;\n  var i := 1;\n  while i < |queue|\n    invariant 0 <= maxIdx < i <= |queue|\n    invariant forall j :: 0 <= j < i ==> queue[j].priority <= queue[maxIdx].priority\n    decreases |queue| - i\n  {\n    if queue[i].priority > queue[maxIdx].priority {\n      maxIdx := i;\n    }\n    i := i + 1;\n  }\n  result := Success(queue[maxIdx]);\n}\n\n// Insert an entry into a sorted queue, maintaining descending priority order\nfunction InsertSorted(sorted: seq<Entry>, e: Entry): seq<Entry>\n  requires IsSortedDesc(sorted)\n  ensures |InsertSorted(sorted, e)| == |sorted| + 1\n  ensures IsSortedDesc(InsertSorted(sorted, e))\n  decreases |sorted|\n{\n  if |sorted| == 0 then [e]\n  else if e.priority >= sorted[0].priority then [e] + sorted\n  else [sorted[0]] + InsertSorted(sorted[1..], e)\n}\n\n// Prioritize: sort the queue by priority (highest first) using insertion sort\nmethod Prioritize(queue: seq<Entry>) returns (sorted: seq<Entry>)\n  ensures |sorted| == |queue|\n  ensures IsSortedDesc(sorted)\n  decreases |queue|\n{\n  sorted := [];\n  var i := 0;\n  while i < |queue|\n    invariant 0 <= i <= |queue|\n    invariant |sorted| == i\n    invariant IsSortedDesc(sorted)\n    decreases |queue| - i\n  {\n    sorted := InsertSorted(sorted, queue[i]);\n    i := i + 1;\n  }\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": "Dafny program verifier finished with 9 verified, 0 errors",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AppointmentScheduler.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-BillingManager.dfy",
    "isVerified": true,
    "moduleName": "BillingManager",
    "dafnySource": "// Auto-composed skeleton for BillingManager\r\n// Pattern: state-machine\r\n// Stubs: none\r\n\r\n// Pattern: State Machine (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Finite states with guarded transitions\n// test: Transition(Idle, \"start\") returns Active\n// test: Transition(Active, \"complete\") returns Completed\n// test: Transition(Idle, \"complete\") returns Idle (invalid transition)\n//\n// Parameters:\n//   states: the datatype variants (customized per domain)\n//   events: the event strings (customized per domain)\n//   guards: which transitions are valid (the match body)\n\ndatatype State =\n  | Idle\n  | Active\n  | Completed\n  | Failed\n\n// Transition: state x event -> state\n// Invalid transitions stay in current state\nmethod Transition(current: State, event: string) returns (next: State)\n  ensures next == Idle || next == Active || next == Completed || next == Failed\n{\n  match current\n  case Idle =>\n    if event == \"start\" {\n      next := Active;\n    } else {\n      next := Idle;\n    }\n  case Active =>\n    if event == \"complete\" {\n      next := Completed;\n    } else if event == \"fail\" {\n      next := Failed;\n    } else {\n      next := Active;\n    }\n  case Completed =>\n    if event == \"reset\" {\n      next := Idle;\n    } else {\n      next := Completed;\n    }\n  case Failed =>\n    if event == \"reset\" {\n      next := Idle;\n    } else {\n      next := Failed;\n    }\n}\n\n// Check if a transition is valid without executing it\nfunction IsValidTransition(current: State, event: string): bool\n{\n  match current\n  case Idle => event == \"start\"\n  case Active => event == \"complete\" || event == \"fail\"\n  case Completed => event == \"reset\"\n  case Failed => event == \"reset\"\n}\n\n// Get all valid events for a state\nmethod GetValidEvents(current: State) returns (events: seq<string>)\n  ensures |events| >= 0\n{\n  if current == Idle {\n    events := [\"start\"];\n  } else if current == Active {\n    events := [\"complete\", \"fail\"];\n  } else if current == Completed {\n    events := [\"reset\"];\n  } else {\n    events := [\"reset\"];\n  }\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": "Dafny program verifier finished with 2 verified, 0 errors",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-BillingManager.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-FhirModels.dfy",
    "isVerified": true,
    "moduleName": "FhirModels",
    "dafnySource": "// Auto-composed skeleton for FhirModels\r\n// Pattern: transformer\r\n// Stubs: console-io\r\n\r\n// Pattern: Transformer (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Transform input through stages to output\n// test: ToUpper([\"hello\", \"world\"]) returns [\"HELLO\", \"WORLD\"]\n// test: Trim([\" a \", \" b \"]) returns [\"a\", \"b\"]\n//\n// Parameters:\n//   operation: the transformation applied per element (ToUpper, ToLower, Trim, etc.)\n//   filterEmpty: bool \u2014 whether to drop empty results (default false)\n\n// Apply a transformation to each element of a sequence\n// SUBSTITUTION_MARKER: operation=ToUpper (replaced by generate-batch.py)\nmethod TransformAll(input: seq<string>) returns (output: seq<string>)\n  requires |input| > 0\n  ensures |output| == |input|\n  decreases |input|\n{\n  output := [];\n  var i := 0;\n  while i < |input|\n    invariant 0 <= i <= |input|\n    invariant |output| == i\n    decreases |input| - i\n  {\n    output := output + [ToUpper(input[i])];\n    i := i + 1;\n  }\n}\n\n// Transform and filter out empty results\nmethod TransformAndFilter(input: seq<string>) returns (output: seq<string>)\n  requires |input| > 0\n  ensures |output| >= 0\n  ensures |output| <= |input|\n  decreases |input|\n{\n  output := [];\n  var i := 0;\n  while i < |input|\n    invariant 0 <= i <= |input|\n    invariant |output| <= i\n    decreases |input| - i\n  {\n    var transformed := ToUpper(input[i]);\n    if |transformed| > 0 {\n      output := output + [transformed];\n    }\n    i := i + 1;\n  }\n}\n\n// Convert string to uppercase\nfunction ToUpper(s: string): string\n  decreases |s|\n{\n  if |s| == 0 then \"\"\n  else if 'a' <= s[0] && s[0] <= 'z' then [CharToUpper(s[0])] + ToUpper(s[1..])\n  else [s[0]] + ToUpper(s[1..])\n}\n\n// Convert lowercase char to uppercase\nfunction CharToUpper(c: char): char\n  ensures 'a' <= c <= 'z' ==> 'A' <= CharToUpper(c) <= 'Z'\n{\n  match c\n  case 'a' => 'A'\n  case 'b' => 'B'\n  case 'c' => 'C'\n  case 'd' => 'D'\n  case 'e' => 'E'\n  case 'f' => 'F'\n  case 'g' => 'G'\n  case 'h' => 'H'\n  case 'i' => 'I'\n  case 'j' => 'J'\n  case 'k' => 'K'\n  case 'l' => 'L'\n  case 'm' => 'M'\n  case 'n' => 'N'\n  case 'o' => 'O'\n  case 'p' => 'P'\n  case 'q' => 'Q'\n  case 'r' => 'R'\n  case 's' => 'S'\n  case 't' => 'T'\n  case 'u' => 'U'\n  case 'v' => 'V'\n  case 'w' => 'W'\n  case 'x' => 'X'\n  case 'y' => 'Y'\n  case 'z' => 'Z'\n  case _ => c\n}\n\n// Reverse a string\nfunction Reverse(s: string): string\n  ensures |Reverse(s)| == |s|\n  decreases |s|\n{\n  if |s| == 0 then \"\"\n  else Reverse(s[1..]) + [s[0]]\n}\r\n\r\n// ---- console-io portals ----\r\n// Stubs: Console I/O portals\n// Pre-cut {:extern} declarations for console operations.\n// Attach to patterns that interact with the user.\n\n// Print a line to console\nmethod {:extern} PrintLine(text: string)\n  ensures true\n\n// Print without newline\nmethod {:extern} Print(text: string)\n  ensures true\n\n// Read a line from console\nmethod {:extern} ReadLine() returns (line: string)\n  ensures |line| >= 0\n\n// Read a character from console\nmethod {:extern} ReadChar() returns (ch: string)\n  ensures |ch| <= 1\n\n// Clear the console\nmethod {:extern} ClearScreen()\n  ensures true\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-FhirModels.dfy(113,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n113 | method {:extern} PrintLine(text: string)\r\n    |                  ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(117,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n117 | method {:extern} Print(text: string)\r\n    |                  ^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(121,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n121 | method {:extern} ReadLine() returns (line: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(125,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n125 | method {:extern} ReadChar() returns (ch: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-FhirModels.dfy(129,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n129 | method {:extern} ClearScreen()\r\n    |                  ^^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 5 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-FhirModels.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AuditTrail.dfy",
    "isVerified": true,
    "moduleName": "AuditTrail",
    "dafnySource": "// Auto-composed skeleton for AuditTrail\r\n// Pattern: repository\r\n// Stubs: time-random\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Repository (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Store items with ID uniqueness, lookup, and update\n// test: Add([], Record(1, \"test\", \"data\")) returns Success([Record(1, \"test\", \"data\")])\n// test: Add([Record(1, \"a\", \"b\")], Record(1, \"c\", \"d\")) returns Failure(\"duplicate id\")\n//\n// Parameters:\n//   idField: string \u2014 name of the ID field (for error messages)\n//   allowUpdate: bool \u2014 whether Add replaces existing IDs (default false)\n\ninclude \"result.dfy\"\n\ndatatype Entity =\n  | Record(id: int, name: string, data: string)\n\n// Invariant: no duplicate IDs\npredicate NoDuplicates(items: seq<Entity>)\n{\n  forall i, j :: 0 <= i < j < |items| ==> items[i].id != items[j].id\n}\n\n// Add: insert if ID not present, fail if duplicate\nmethod Add(items: seq<Entity>, entity: Entity) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| == |items| + 1\n  ensures result.Failure? ==> result.error == \"duplicate id\"\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    invariant NoDuplicates(items)\n    decreases |items| - i\n  {\n    if items[i].id == entity.id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    result := Failure(\"duplicate id\");\n  } else {\n    result := Success(items + [entity]);\n  }\n}\n\n// Find: lookup by ID\nmethod Find(items: seq<Entity>, id: int) returns (result: Result<Entity>)\n  ensures result.Success? ==> result.value.id == id\n  decreases |items|\n{\n  var i := 0;\n  while i < |items|\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      result := Success(items[i]);\n      return;\n    }\n    i := i + 1;\n  }\n  result := Failure(\"not found\");\n}\n\n// Remove: delete by ID\nmethod Remove(items: seq<Entity>, id: int) returns (result: Result<seq<Entity>>)\n  requires NoDuplicates(items)\n  ensures result.Success? ==> |result.value| <= |items|\n  decreases |items|\n{\n  var i := 0;\n  var found := false;\n  while i < |items| && !found\n    invariant 0 <= i <= |items|\n    decreases |items| - i\n  {\n    if items[i].id == id {\n      found := true;\n    }\n    i := i + 1;\n  }\n\n  if found {\n    var newItems := [];\n    var j := 0;\n    while j < |items|\n      invariant 0 <= j <= |items|\n      invariant |newItems| <= j\n      decreases |items| - j\n    {\n      if items[j].id != id {\n        newItems := newItems + [items[j]];\n      }\n      j := j + 1;\n    }\n    result := Success(newItems);\n  } else {\n    result := Failure(\"not found\");\n  }\n}\r\n\r\n// ---- time-random portals ----\r\n// Stubs: Time and Random portals\n// Pre-cut {:extern} declarations for non-deterministic operations.\n// Attach to patterns that need timestamps, delays, or randomness.\n\n// Get current time as Unix timestamp (seconds since epoch)\nmethod {:extern} GetCurrentTime() returns (timestamp: int)\n  ensures timestamp > 0\n\n// Get current time as ISO string\nmethod {:extern} GetIsoTimestamp() returns (timestamp: string)\n  ensures |timestamp| > 0\n\n// Sleep for N milliseconds\nmethod {:extern} Sleep(milliseconds: int)\n  requires milliseconds >= 0\n\n// Get a random integer in range [min, max)\nmethod {:extern} RandomInt(min: int, max: int) returns (value: int)\n  requires min < max\n  ensures min <= value < max\n\n// Get a random boolean\nmethod {:extern} RandomBool() returns (value: bool)\n  ensures true\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-AuditTrail.dfy(116,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n116 | method {:extern} GetCurrentTime() returns (timestamp: int)\r\n    |                  ^^^^^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-AuditTrail.dfy(120,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n120 | method {:extern} GetIsoTimestamp() returns (timestamp: string)\r\n    |                  ^^^^^^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-AuditTrail.dfy(128,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n128 | method {:extern} RandomInt(min: int, max: int) returns (value: int)\r\n    |                  ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-AuditTrail.dfy(133,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n133 | method {:extern} RandomBool() returns (value: bool)\r\n    |                  ^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 7 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-AuditTrail.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-IntegrationGateway.dfy",
    "isVerified": true,
    "moduleName": "IntegrationGateway",
    "dafnySource": "// Auto-composed skeleton for IntegrationGateway\r\n// Pattern: adapter\r\n// Stubs: network-io\r\n\r\ninclude \"result.dfy\"\r\n\r\n// Pattern: Adapter (Approach 3 \u2014 pre-written body with parameters)\n// responsibility: Wrap an external API in a clean interface\n// test: SendRequest(SimpleRequest(\"GET\", \"/users\")) returns Response(200, \"[]\")\n// test: SendRequest(FullRequest(\"POST\", \"/users\", \"data\")) returns Response(201, \"created\")\n//\n// Parameters:\n//   baseUrl: string \u2014 the base URL for the external API (default \"https://api.example.com\")\n//   timeoutMs: int \u2014 request timeout in milliseconds (default 5000)\n//   defaultHeaders: seq<string> \u2014 headers added to every request (default [])\n\ninclude \"result.dfy\"\n\ndatatype Request =\n  | SimpleRequest(verb: string, path: string)\n  | FullRequest(verb: string, path: string, body: string)\n\ndatatype Response =\n  | Response(status: int, body: string)\n\n// Send a single request through the adapter\nmethod SendRequest(req: Request) returns (resp: Response)\n  ensures resp.Response?\n{\n  if req.SimpleRequest? {\n    if req.verb == \"GET\" {\n      resp := Response(200, \"[]\");\n    } else {\n      resp := Response(200, \"ok\");\n    }\n  } else if req.FullRequest? {\n    if req.verb == \"POST\" {\n      resp := Response(201, \"created\");\n    } else if req.verb == \"PUT\" {\n      resp := Response(200, \"updated\");\n    } else if req.verb == \"DELETE\" {\n      resp := Response(204, \"\");\n    } else {\n      resp := Response(400, \"bad method\");\n    }\n  } else {\n    resp := Response(400, \"unknown request type\");\n  }\n}\n\n// Send multiple requests in batch, collecting responses\nmethod BatchSend(requests: seq<Request>) returns (responses: seq<Response>)\n  requires |requests| > 0\n  ensures |responses| == |requests|\n  decreases |requests|\n{\n  responses := [];\n  var i := 0;\n  while i < |requests|\n    invariant 0 <= i <= |requests|\n    invariant |responses| == i\n    decreases |requests| - i\n  {\n    var r := SendRequest(requests[i]);\n    responses := responses + [r];\n    i := i + 1;\n  }\n}\n\n// Send with retry: attempts the request up to maxAttempts times\nmethod SendWithRetry(req: Request, maxAttempts: int) returns (result: Result<Response>)\n  requires maxAttempts > 0\n  ensures result.Success? || result.Failure?\n  ensures result.Success? ==> result.value.Response?\n  ensures result.Failure? ==> result.error == \"all attempts failed\"\n  decreases maxAttempts\n{\n  var attempts := 0;\n  result := Failure(\"all attempts failed\");\n  while attempts < maxAttempts\n    invariant 0 <= attempts <= maxAttempts\n    invariant result.Failure? ==> result.error == \"all attempts failed\"\n    decreases maxAttempts - attempts\n  {\n    var resp := SendRequest(req);\n    if resp.status >= 200 && resp.status < 300 {\n      result := Success(resp);\n      return;\n    }\n    attempts := attempts + 1;\n  }\n}\n\n// Validate a request before sending\nmethod ValidateRequest(req: Request) returns (result: Result<Request>)\n  ensures result.Success? ==> result.value == req\n  ensures result.Failure? ==> |result.error| > 0\n{\n  if req.SimpleRequest? {\n    if |req.verb| == 0 {\n      result := Failure(\"method is empty\");\n    } else if |req.path| == 0 {\n      result := Failure(\"path is empty\");\n    } else if req.path[0] != '/' {\n      result := Failure(\"path must start with /\");\n    } else {\n      result := Success(req);\n    }\n  } else if req.FullRequest? {\n    if |req.verb| == 0 {\n      result := Failure(\"method is empty\");\n    } else if |req.path| == 0 {\n      result := Failure(\"path is empty\");\n    } else if req.path[0] != '/' {\n      result := Failure(\"path must start with /\");\n    } else {\n      result := Success(req);\n    }\n  } else {\n    result := Failure(\"unknown request type\");\n  }\n}\n\n// Count successful responses (status 200-299) in a batch\nmethod CountSuccessful(responses: seq<Response>) returns (count: int)\n  ensures count >= 0\n  ensures count <= |responses|\n  decreases |responses|\n{\n  count := 0;\n  var i := 0;\n  while i < |responses|\n    invariant 0 <= i <= |responses|\n    invariant count <= i\n    decreases |responses| - i\n  {\n    if responses[i].status >= 200 && responses[i].status < 300 {\n      count := count + 1;\n    }\n    i := i + 1;\n  }\n}\r\n\r\n// ---- network-io portals ----\r\n// Stubs: Network I/O portals\n// Pre-cut {:extern} declarations for HTTP and network operations.\n// Attach to patterns that make web requests or API calls.\n\n// HTTP GET request\nmethod {:extern} HttpGet(url: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// HTTP POST request\nmethod {:extern} HttpPost(url: string, body: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// HTTP PUT request\nmethod {:extern} HttpPut(url: string, body: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// HTTP DELETE request\nmethod {:extern} HttpDelete(url: string) returns (response: string)\n  requires |url| > 0\n  ensures |response| >= 0\n\n// Open a TCP connection\nmethod {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\n  requires |host| > 0\n  requires port > 0\n  ensures socketId >= 0\n\n// Send data over socket\nmethod {:extern} SocketSend(socketId: int, data: string)\n  requires socketId >= 0\n\n// Receive data from socket\nmethod {:extern} SocketRecv(socketId: int) returns (data: string)\n  requires socketId >= 0\n  ensures |data| >= 0\n\n// Close socket\nmethod {:extern} CloseSocket(socketId: int)\n  requires socketId >= 0\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-IntegrationGateway.dfy(150,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n150 | method {:extern} HttpGet(url: string) returns (response: string)\r\n    |                  ^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(155,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n155 | method {:extern} HttpPost(url: string, body: string) returns (response: string)\r\n    |                  ^^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(160,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n160 | method {:extern} HttpPut(url: string, body: string) returns (response: string)\r\n    |                  ^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(165,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n165 | method {:extern} HttpDelete(url: string) returns (response: string)\r\n    |                  ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(170,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n170 | method {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\r\n    |                  ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-IntegrationGateway.dfy(180,17): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n    |\r\n180 | method {:extern} SocketRecv(socketId: int) returns (data: string)\r\n    |                  ^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 7 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-IntegrationGateway.cs"
  },
  {
    "dafnyPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-WebApi.dfy",
    "isVerified": true,
    "moduleName": "WebApi",
    "dafnySource": "// Auto-composed io-shell skeleton for WebApi\r\n// Stubs: network-io, console-io\r\n\r\nmodule WebApi {\r\n\r\n  // ---- network-io portals ----\r\n  // Stubs: Network I/O portals\r\n  // Pre-cut {:extern} declarations for HTTP and network operations.\r\n  // Attach to patterns that make web requests or API calls.\r\n\r\n  // HTTP GET request\r\n  method {:extern} HttpGet(url: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // HTTP POST request\r\n  method {:extern} HttpPost(url: string, body: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // HTTP PUT request\r\n  method {:extern} HttpPut(url: string, body: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // HTTP DELETE request\r\n  method {:extern} HttpDelete(url: string) returns (response: string)\r\n    requires |url| > 0\r\n    ensures |response| >= 0\r\n\r\n  // Open a TCP connection\r\n  method {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\r\n    requires |host| > 0\r\n    requires port > 0\r\n    ensures socketId >= 0\r\n\r\n  // Send data over socket\r\n  method {:extern} SocketSend(socketId: int, data: string)\r\n    requires socketId >= 0\r\n\r\n  // Receive data from socket\r\n  method {:extern} SocketRecv(socketId: int) returns (data: string)\r\n    requires socketId >= 0\r\n    ensures |data| >= 0\r\n\r\n  // Close socket\r\n  method {:extern} CloseSocket(socketId: int)\r\n    requires socketId >= 0\r\n\r\n  // ---- console-io portals ----\r\n  // Stubs: Console I/O portals\r\n  // Pre-cut {:extern} declarations for console operations.\r\n  // Attach to patterns that interact with the user.\r\n\r\n  // Print a line to console\r\n  method {:extern} PrintLine(text: string)\r\n    ensures true\r\n\r\n  // Print without newline\r\n  method {:extern} Print(text: string)\r\n    ensures true\r\n\r\n  // Read a line from console\r\n  method {:extern} ReadLine() returns (line: string)\r\n    ensures |line| >= 0\r\n\r\n  // Read a character from console\r\n  method {:extern} ReadChar() returns (ch: string)\r\n    ensures |ch| <= 1\r\n\r\n  // Clear the console\r\n  method {:extern} ClearScreen()\r\n    ensures true\r\n}\r\n",
    "verifiedTypes": [],
    "contractSummary": "Pre-verified from pattern registry (no model call needed)",
    "verificationOutput": ".posit/staging/skeleton-WebApi.dfy(12,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n12 |   method {:extern} HttpGet(url: string) returns (response: string)\r\n   |                    ^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(17,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n17 |   method {:extern} HttpPost(url: string, body: string) returns (response: string)\r\n   |                    ^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(22,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n22 |   method {:extern} HttpPut(url: string, body: string) returns (response: string)\r\n   |                    ^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(27,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n27 |   method {:extern} HttpDelete(url: string) returns (response: string)\r\n   |                    ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(32,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n32 |   method {:extern} OpenSocket(host: string, port: int) returns (socketId: int)\r\n   |                    ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(42,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n42 |   method {:extern} SocketRecv(socketId: int) returns (data: string)\r\n   |                    ^^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(56,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n56 |   method {:extern} PrintLine(text: string)\r\n   |                    ^^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(60,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n60 |   method {:extern} Print(text: string)\r\n   |                    ^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(64,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n64 |   method {:extern} ReadLine() returns (line: string)\r\n   |                    ^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(68,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n68 |   method {:extern} ReadChar() returns (ch: string)\r\n   |                    ^^^^^^^^\r\n\r\n.posit/staging/skeleton-WebApi.dfy(72,19): Warning: when a method is imported, meaning it has no body and an {:extern} annotation, Dafny can not guarantee that its implementation satisfies its post-conditions (its ensures clauses and outputs that are subset types). To silence this warning, please add an {:axiom} attribute or use the option '--allow-external-contracts'.\n   |\r\n72 |   method {:extern} ClearScreen()\r\n   |                    ^^^^^^^^^^^\r\n\r\n\r\nDafny program verifier finished with 0 verified, 0 errors\r\nCompilation failed because warnings were found and --allow-warnings is false",
    "translatedCSharpPath": "C:\\Users\\goldf\\Posit\\.posit\\staging\\skeleton-WebApi.cs"
  }
]